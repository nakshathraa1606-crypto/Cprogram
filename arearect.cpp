#include<stdio.h>
int main(){
	int l,b,a;
	printf("the length is",l);
	scanf("%d",&l);
	printf("the breadth is",b);
	scanf("%d",&b);
	a=l*b;
	printf("the area of rectangle is %d",a);
	return 0;
}